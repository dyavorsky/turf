# R package compiled files (machine-specific, regenerated at install time)
src/*.o
src/*.so
src/*.dll

# RStudio
*.Rproj
.Rproj.user/

# R environment / history
.RData
.Rhistory
.Rapp.history

# renv (if used in this directory — analysis projects use renv, not the package itself)
renv/
renv.lock

# OS
.DS_Store
Thumbs.db
